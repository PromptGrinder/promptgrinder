# v1.0.0 final release gate

Decision: **NO-GO until the pending external gates are reviewed.**

The source reports `v1.0.0`, and local source qualification is recorded in
[`v1.0.0-qualification.md`](v1.0.0-qualification.md). The version-bump branch is
not a release: no `v1.0.0` tag or published release exists as a result of this
pull request.

## Completed source gates

1. Embedded application version and assertions agree on `v1.0.0`.
2. README, changelog, security policy, use cases, release notes, and source-only
   distribution policy describe the final line consistently.
3. Formatting, vet, module verification, ordinary tests, race tests, compiled
   CLI acceptance, source-only policy, Homebrew automation tests, YAML parsing,
   and Darwin cross-build inspection pass locally.
4. Compatibility review found no schema migration or intentional v1 contract
   break.

## Pending external gates

1. The App-authored pull request must pass GitHub CI and receive human review.
2. The merged commit and worktree must be clean and identified exactly.
3. The clean-machine checklist must record Homebrew installation and supported
   runtime behavior on the claimed macOS/architecture matrix.
4. A maintainer must explicitly authorize the annotated `v1.0.0` tag.
5. The tag-triggered qualification workflow must pass and its draft release
   must be reviewed before publication.

## Publication boundary

Do not merge this pull request automatically. Do not create or push the tag as
part of version preparation. Do not publish a release until every pending gate
has reviewed evidence. Publishing the approved release may trigger a separate
Homebrew formula pull request; tap CI and manual merge remain mandatory.
