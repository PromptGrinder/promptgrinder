# v1.0.0-rc.2.1 qualification record

Status: release-prepared candidate; artifact qualification pending.

Use the [clean-machine checklist](clean-machine-checklist.md). Record only
evidence from exact candidate artifacts; source-tree checks do not qualify a
release artifact.

## Source evidence

| Check | Result |
| --- | --- |
| Focused Codex engine, runtime, and folder-runner tests | pass |
| Full `go test ./...` | pass |
| Full `go test -race -count=1 ./...` | pass |
| `go vet ./...` | pass |
| `git diff --check` | pass |

## RC.2.1 regression gates

| Contract | Expected result | Status |
| --- | --- | --- |
| Large Codex JSONL event | final completion after a record over 64 KiB is parsed | source test passes |
| Completion ordering | terminal worker state includes parsed completion evidence | source test passes |
| Existing ordered runs | strict PASS/yes contract and stop behavior remain intact | full source suite passes |
| Artifact smoke | exact tagged native binary reports RC.2.1 and completes smoke run | pending |

## Artifact matrix

| Target | Result | Evidence |
| --- | --- | --- |
| macOS arm64 | pending | tagged archive and native smoke required |
| macOS amd64 | pending | tagged archive and native smoke required |
| Codex folder run with large tool output | pending | exact candidate binary required |

## Current blockers

- The candidate has not been tagged.
- Exact release artifacts and checksums do not exist.
- Native and clean-machine qualification is pending.
- Publication requires explicit maintainer approval.
