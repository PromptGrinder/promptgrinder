# v1.0.0-rc.4.0 qualification record

Status: candidate source qualification; no RC.4.0 tag or release created.
RC.2.4 remains the published Homebrew baseline while RC.4.0 is qualified.

## Required source checks

Run from a clean checkout with isolated PromptGrinder and Go state:

```sh
PROMPTGRINDER_HOME="$(mktemp -d)" GOCACHE=/tmp/promptgrinder-go-cache go test -shuffle=on -count=1 ./...
PROMPTGRINDER_HOME="$(mktemp -d)" GOCACHE=/tmp/promptgrinder-go-cache go test -race -count=1 ./...
GOCACHE=/tmp/promptgrinder-go-cache go vet ./...
go mod verify
test -z "$(gofmt -l .)"
git diff --check
```

The candidate must build the real CLI and verify `--version`, `--help`,
work-order validation, run-folder dry-run, JSON, and exit-code behavior without
invoking a real AI service or GUI terminal.

Cross-build `darwin/arm64` and `darwin/amd64` with `CGO_ENABLED=0` and
`-trimpath`. Both outputs must be nonempty Mach-O executables; only the native
artifact is executed during local qualification.

## Compatibility review

- Existing work orders default to `context_mode: shared`, preserving their
  session-resume behavior.
- `context_mode: fresh` is additive and receives the same specification,
  repository state, role policy, and path constraints as a shared slice.
- The task-frontmatter semantic preamble changes from v2 to v3; unknown
  frontmatter remains rejected.
- Run-folder sequence state accepts existing records; the additive selected
  context mode is persisted for newly created or refreshed records.
- No compiled release archives are restored.

Exact clean-machine macOS, Codex, Terminal.app, and iTerm2 qualification
remains a human release gate after merge and before tagging.
