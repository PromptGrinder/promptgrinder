# v1.0.0-rc.4.2 final release gate

The source reports `v1.0.0-rc.4.2`. Merging this candidate does not tag or
publish it.

Before any release action:

1. Complete the RC.4.2 qualification commands successfully.
2. Confirm `promptgrinder --version` reports `v1.0.0-rc.4.2`.
3. Review recovery artifacts for their preservation-only behavior; confirm no
   reset, clean, stash, automatic partial commit, or broad process cleanup was
   introduced.
4. Obtain explicit authorization before creating and pushing a
   `v1.0.0-rc.4.2` tag or publishing a GitHub prerelease.
