# v1.0.0 qualification record

Status: source qualification complete locally on 2026-08-02; GitHub CI,
review, tagged-source, and clean-machine evidence pending.

This record covers the final-version pull-request source. It does not qualify a
future tag by inference. Repeat tag-dependent and clean-machine checks against
the exact reviewed `v1.0.0` commit before publication.

## Source evidence

| Check | Result |
| --- | --- |
| `go test -shuffle=on -count=1 ./...` | pass |
| `go test -race -count=1 ./...` | pass |
| `go vet ./...` | pass |
| `go mod verify` | pass |
| `test -z "$(gofmt -l .)"` | pass |
| `git diff --check` | pass |
| Homebrew update contract | pass |
| Source-only release contract | pass |
| All repository YAML parses | pass |

## Compiled CLI evidence

The real CLI was built with `-trimpath` and exercised locally for exact
`v1.0.0` version output, help, prompt validation, dry-run behavior, JSON output,
and documented exit-code contracts through the compiled acceptance suite.

Ephemeral `CGO_ENABLED=0` binaries were built for Darwin arm64 and amd64 and
verified as nonempty Mach-O executables. Only the native architecture was
executed. These binaries are qualification evidence and are not GitHub release
assets.

## Compatibility review

- No command, JSON, task-frontmatter, project configuration, or persisted-state
  schema changes are introduced by the version bump.
- Existing RC state remains readable; no migration or destructive setup is
  required.
- The improved discovery conflict is error-message-only and retains the
  existing fail-closed, no-overwrite behavior and exit code.
- Commit-ownership diagnostics require one exact approved worker commit and a
  clean worktree; unrelated paths, multiple commits, residual changes, and
  staged mismatches retain the generic refusal without rewriting Git state.
- First-use guidance and setup capability inventory are read-only until the
  existing explicit setup confirmation; active terminal probes remain opt-in.
- GitHub releases remain source-only. Homebrew update automation remains a
  separate, review-first workflow.

## Pending external evidence

- GitHub pull-request CI on the exact App-authored commit.
- Human review and approval of this final-version pull request.
- Clean-machine Homebrew installation and runtime checks on the claimed macOS
  and architecture matrix.
- Annotated-tag verification and final publication approval.
