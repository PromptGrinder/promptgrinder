# v1.0.0-rc.2.4 qualification record

Status: candidate source qualification; no tag or release created.

## Required source checks

Run from a clean checkout with isolated PromptGrinder and Go state:

```sh
PROMPTGRINDER_HOME="$(mktemp -d)" GOCACHE=/tmp/promptgrinder-go-cache go test -shuffle=on -count=1 ./...
PROMPTGRINDER_HOME="$(mktemp -d)" GOCACHE=/tmp/promptgrinder-go-cache go test -race -count=1 ./...
GOCACHE=/tmp/promptgrinder-go-cache go vet ./...
go mod verify
test -z "$(gofmt -l .)"
git diff --check
```

The candidate must build the real CLI and verify `--version`, `--help`, and
letter-suffixed ordered-slice discovery without invoking a real AI service or a
GUI terminal.

## Compatibility review

- Existing numeric and typed filenames retain their behavior.
- `NN[A-Z]*-*.md` is additive filename support; it does not change sorting,
  dependency ordering, persisted state, public commands, or exit codes.
- Generic letter-suffixed filenames still require stable `id` and explicit
  `type` frontmatter.
- No compiled release archives are restored.

Exact clean-machine macOS, Codex, Terminal.app, and iTerm2 qualification
remains a human release gate after merge and before tagging.
