# v1.0.0-rc.5.2 qualification record

Status: locally qualified candidate source; tag and release pending.

## Required source checks

```sh
PROMPTGRINDER_HOME="$(mktemp -d)" GOCACHE=/tmp/promptgrinder-go-cache go test -shuffle=on -count=1 ./...
PROMPTGRINDER_HOME="$(mktemp -d)" GOCACHE=/tmp/promptgrinder-go-cache go test -race -count=1 ./...
GOCACHE=/tmp/promptgrinder-go-cache go vet ./...
go mod verify
test -z "$(gofmt -l .)"
git diff --check
```

Build the real CLI and verify `--version`, `--help`, validation, dry-run, JSON,
and exit-code behavior without a real AI service or GUI terminal. Cross-build
Darwin arm64 and amd64 with `CGO_ENABLED=0` and `-trimpath`; verify nonempty
Mach-O outputs and execute only the native artifact.

## Recorded local results (2026-08-24)

- `go test -shuffle=on -count=1 ./...`: passed.
- `go test -race -count=1 ./...`: passed.
- `go vet ./...`, `go mod verify`, `gofmt`, and `git diff --check`: passed.
- `scripts/verify-source-only-release.sh` and
  `scripts/test-homebrew-formula-update.sh`: passed.
- Built CLI smoke checks passed for `--version`, `--help`, validation, JSON
  validation, dry-run, and invalid-path non-zero exit behavior without invoking
  Codex.
- `CGO_ENABLED=0 -trimpath` cross-builds produced nonempty Darwin arm64 and
  amd64 Mach-O executables. The native artifact was exercised by the CLI smoke
  checks.

## Compatibility review

- Failure-report fields are additive in worker results, sequence items, and
  progress events; old worker and sequence state remains readable.
- An absent failure report receives a conservative terminal classification but
  does not alter existing failed-worker, checkpoint, recovery, or clean-Git
  behavior.
- `gate_outcome: BLOCKED` remains the sole declared exception to ordinary
  blocked completion handling. Undeclared blocked, partial, malformed,
  cancelled, timeout, engine, and path-policy failures remain non-continuable.
