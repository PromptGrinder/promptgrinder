# v1.0.0-rc.2.3 qualification record

Status: candidate source qualification; no tag or release created.

## Required source checks

Run from a clean checkout with isolated PromptGrinder and Go state:

```sh
PROMPTGRINDER_HOME="$(mktemp -d)" GOCACHE=/tmp/promptgrinder-go-cache go test -shuffle=on -count=1 ./...
PROMPTGRINDER_HOME="$(mktemp -d)" GOCACHE=/tmp/promptgrinder-go-cache go test -race -count=1 ./...
GOCACHE=/tmp/promptgrinder-go-cache go vet ./...
go mod verify
test -z "$(gofmt -l .)"
git diff --check
```

The candidate must also build the real CLI and verify `--version`, `--help`,
compiled acceptance coverage, deterministic examples, source-only release
configuration, and Homebrew update validation without invoking a real AI
service or GUI terminal.

## Compatibility review

- No public command or exit-code contract is intentionally removed.
- Existing sequence records remain readable; new repository-path and
  cancellation fields are additive.
- Legacy feature-local run state remains readable while new state is stored
  below `PROMPTGRINDER_HOME`.
- Existing exact path rules retain exact semantics; newly generated directory
  policies use explicit `/**` patterns.
- No compiled release archives are restored.

Exact clean-machine macOS, Codex, Terminal.app, and iTerm2 qualification remains
a human release gate after merge and before tagging.
