# v1.0.0-rc.5.2 final release gate

The source reports `v1.0.0-rc.5.2`. Merging this candidate does not tag or
publish it.

Before creating the annotated tag:

1. Complete the RC.5.2 qualification commands successfully from the exact
   reviewed release commit.
2. Confirm `promptgrinder --version` reports `v1.0.0-rc.5.2`.
3. Review the additive failure-report JSON/state fields and their compatibility
   behavior, including the absence-of-report fallback.
4. Confirm the Homebrew workflow remains source-only and requires a published
   release before it opens a tap pull request.
5. Ensure the worktree is clean with no generated binary, PromptGrinder state,
   credential, private prompt, or developer-specific path tracked.
6. Obtain explicit authorization before creating and pushing the annotated
   `v1.0.0-rc.5.2` tag or publishing a GitHub prerelease.
