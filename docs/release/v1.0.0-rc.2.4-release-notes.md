# PromptGrinder v1.0.0-rc.2.4 candidate notes

Status: candidate source; not tagged or published.

RC.2.4 relaxes ordered slice discovery without changing execution ordering or
dependency safety.

## Highlights

- `run-folder` accepts an optional uppercase letter suffix after a two-digit
  ordering number. Both `08A-ranking-history.md` and
  `08A-implement-ranking-history.md` are recognized.
- Letter-suffixed generic names require the same explicit `id` and `type`
  frontmatter as other generic ordered names.
- Filename sorting remains deterministic, and `depends_on` entries must still
  resolve to an earlier slice in that order.
- `run-folder` now injects declared role guidance and enforces role paths as an
  outer boundary. Slice validation remains slice-owned, so role readiness gates
  do not turn intermediate backend work into broad Android or release checks.
- Explicit `--resume` preserves an unchanged completed prefix after a later
  slice or role-policy repair, rather than forcing successful work to rerun.
- The Slice DSL reference documents the filename grammar, frontmatter contract,
  path policy, and completion report.
- Foreground `run-folder` uses the same terminal spinner as `run`, and failed
  rows show a copyable absolute worker-log path with a local file hyperlink
  where supported.
- Repository-owned `.promptgrinder/models.yaml` can now constrain model choice
  by low, medium, or high cost tier and declared capabilities. Roles can supply
  defaults and slices can make bounded overrides.
- PromptGrinder validates resolved Codex models against the active runtime
  catalog before a worker or detached sequence starts. It fails closed for an
  unavailable, unapproved, over-budget, or incompatible model and never falls
  back to a more expensive model.
- Role path boundaries remain enforced alongside model selection, so a scoped
  documentation or CI task cannot widen itself into production code changes.
- Ordered folders can opt into one to three bounded automatic recovery retries
  for a recoverable failed slice. The completed prefix is preserved and the
  retry receives the prior failure context; non-passing completions retry only
  the same slice, while model selection, preflight, path-policy, cancellation,
  and required-clean-baseline failures remain explicit stops.
- `validate --repo` now makes external task folders unambiguous by showing the
  resolved repository, role/slice boundary, model cost/capabilities, and
  sandbox level. `validate-folder` performs full ordered-folder preflight
  without creating a worker or sequence.

## Distribution

GitHub releases remain source-only. Merging this candidate does not create a
tag or publish a release.
