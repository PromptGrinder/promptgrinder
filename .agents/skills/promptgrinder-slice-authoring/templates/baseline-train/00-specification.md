# <Feature name> — shared specification

## Outcome

Describe the user-visible outcome, non-goals, compatibility expectations, and
the source of truth for product decisions.

## Prerequisites

List required committed handoffs, existing contracts, capabilities, and the
commands or evidence that establish them. State where a missing prerequisite
must be repaired rather than inferred.

## Slice boundaries

- `10-implement-<feature>.pg`: <bounded implementation outcome>.
- `20-verify-<feature>.pg`: <focused and final verification outcome>.

## Final gate

List the complete repository/component checks required before the feature can
be reported as complete.
